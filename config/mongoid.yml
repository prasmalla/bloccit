development:
  host: localhost
  database: bloccit_development

test:
  host: localhost
  database: bloccit_test

production:
  sessions:
    default:
      uri: <%= ENV['MONGOLAB_URI'] %>
        options:
          skip_version_check: true
          safe: true
